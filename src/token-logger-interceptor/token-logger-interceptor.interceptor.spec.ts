import { TokenLoggerInterceptorInterceptor } from './token-logger-interceptor.interceptor';

describe('TokenLoggerInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new TokenLoggerInterceptorInterceptor()).toBeDefined();
  });
});
